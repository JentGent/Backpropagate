`neural-network.js` has a small Neural Network class. This is where I do backpropagation. It only has one hidden layer  
`training data.txt` is a text file with training data  
`index.html` has a simple GUI for setting up and training the neural network with the training data  
